Nice to haves:
    1. text is rendered not like text, but in MD format (or something to show bold, code, citations and so on),
        so it would be easier to read and understand important places. Of course LLM would need to write in such format.
    2. an option in (set default in settings and can override per analysis) to disable specific analysis parts
        like risks, descriptions, grouping modes (get only one), verbosity. If something is disabled - just remove info about it in the prompt 
        and say clearly that this is not needed (so LLM would have less work)
    3. Add an option to merge interface + it's implementations in change diagram (like 2 nodes in one)
    4. Add test LLM connection inside the form where we input the LLM API data
    5. Add user instructions. Add a button help with information, FAQ and step by step guide (not screenshots, real tour in UI)