namespace DiffHacker.Storage;

/// <summary>
/// SQLite persistence, the analysis library, settings and secrets.
/// <para>
/// Placeholder anchor so the project, its references and the architecture tests exist from
/// Iteration 1. Delete this type once the project has real content.
/// </para>
/// </summary>
public static class AssemblyMarker;
