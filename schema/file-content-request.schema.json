{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://diffhacker.dev/schema/1.3/file-content-request.schema.json",
  "title": "FileContentRequest",
  "description": "Parameters of changeset.fileContent. One file, one side of the comparison, on demand.",
  "type": "object",
  "additionalProperties": false,
  "required": ["repositoryPath", "path", "side"],
  "properties": {
    "repositoryPath": {
      "type": "string",
      "description": "Absolute path to the worktree root."
    },
    "path": {
      "type": "string",
      "description": "Repository-relative path of the file."
    },
    "side": {
      "type": "string",
      "description": "head reads the file as committed; working_tree reads it as it is on disk right now.",
      "enum": ["head", "working_tree"]
    }
  }
}
