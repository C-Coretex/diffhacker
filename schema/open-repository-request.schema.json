{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://diffhacker.dev/schema/1.5/open-repository-request.schema.json",
  "title": "OpenRepositoryRequest",
  "description": "Parameters for the repository.open method. The path may be any directory inside a working tree; the host resolves it to the worktree root.",
  "type": "object",
  "additionalProperties": false,
  "required": ["path"],
  "properties": {
    "path": {
      "type": "string",
      "description": "Filesystem path the user chose, from the native folder picker or typed by hand."
    }
  }
}
