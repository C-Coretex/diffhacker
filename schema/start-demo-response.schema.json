{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://diffhacker.dev/schema/1.0/start-demo-response.schema.json",
  "title": "StartDemoResponse",
  "description": "Result of demo.startCountdown. Carries the operation identifier the subsequent ProgressNotification stream is tagged with.",
  "type": "object",
  "additionalProperties": false,
  "required": ["operationId", "totalSteps"],
  "properties": {
    "operationId": {
      "type": "string",
      "description": "Identifier every ProgressNotification for this operation carries."
    },
    "totalSteps": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of notifications the caller should expect."
    }
  }
}
